use leptos::prelude::*;

#[component]
pub fn PluginsPage() -> impl IntoView {
    view! {
        <section class="ferro-plugins">
            <h1>"Plugins"</h1>
            <p>"Install, configure, and grant capabilities to WASM plugins."</p>
        </section>
    }
}
