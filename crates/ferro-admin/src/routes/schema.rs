use leptos::prelude::*;

#[component]
pub fn SchemaBuilder() -> impl IntoView {
    view! {
        <section class="ferro-schema">
            <h1>"Schema"</h1>
            <p>"Define content types and fields."</p>
        </section>
    }
}
